# Tuples: is created by placing elements inside () and seprated by ','.
# these are immutable.

v = ('V', 'VV', 'V2')

# functions
# tuple(seq):  use to typecast the tuple sequence.

# rest all methods and functions are same except list(seq)